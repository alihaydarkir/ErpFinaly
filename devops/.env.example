# ============================================
# ERP SYSTEM - DOCKER ENVIRONMENT
# ============================================
# Copy this file to .env and customize for your environment

# Database Configuration
DB_USER=postgres
DB_PASSWORD=12345
DB_NAME=erp_db

# JWT Configuration (REQUIRED - Generate a secure 32+ character secret)
# Generate: openssl rand -base64 48
JWT_SECRET=ThisIsMyVerySecretKeyForJWTTokensMinimum32Characters
JWT_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Application Configuration
NODE_ENV=production
FRONTEND_URL=http://localhost:5000

# Ollama AI Configuration
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_MODEL=llama2

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# ============================================
# PRODUCTION DEPLOYMENT VARIABLES
# ============================================
# Domain Configuration
DOMAIN=helix-ware.com
SSL_EMAIL=admin@helix-ware.com

# Let's Encrypt SSL
LETSENCRYPT_TEST=true

# ============================================
# SECURITY TIPS
# ============================================
# Generate secure secrets:
# JWT_SECRET: openssl rand -base64 48
# DB_PASSWORD: openssl rand -base64 32
